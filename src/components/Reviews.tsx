import { business } from '../data/business'
const initials = (name: string) => name.split(' ').filter(Boolean).slice(0, 2).map(word => word[0]).join('')

export default function Reviews() { const score = business.rating.toFixed(1).replace('.', ','); return <section id="avaliacoes" className="section reviews"><div className="reviews-head"><div><p className="eyebrow">Avaliações</p><h2>Confiança percebida em cada atendimento.</h2><p className="reviews-intro">Relatos publicados por clientes no perfil do escritório no Google.</p></div><div className="google-score"><strong>{score}</strong><span aria-label="5 de 5 estrelas">★★★★★</span><small>{business.reviewCount} avaliações no Google</small></div></div><div className="review-grid">{business.reviews.map(review => <figure key={review.name}><div className="review-top"><span className="review-avatar" aria-hidden="true">{initials(review.name)}</span><div><figcaption>{review.name}</figcaption><small>Avaliação no Google</small></div></div><div className="review-stars" aria-label={`${review.rating} de 5 estrelas`}>★★★★★</div><blockquote>“{review.text}”</blockquote></figure>)}</div><a className="reviews-link" href={business.mapsUrl} target="_blank" rel="noreferrer">Ver perfil no Google <span>↗</span></a></section> }
